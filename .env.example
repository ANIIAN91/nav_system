# Database Configuration
# Option 1: Use individual variables (recommended, auto-encodes special characters)
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nav_system

# Option 2: Use full DATABASE_URL (must manually URL-encode special chars like @ -> %40)
# DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/nav_system

# Security Configuration (REQUIRED)
SECRET_KEY=generate-a-random-32-char-string-here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_password
